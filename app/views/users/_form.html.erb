<%= simple_form_for @user, defaults: { input_html: { class: 'mb-3' } } do |f|%>
  <%= f.input :email %>
  <%= f.input :name %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>

  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
